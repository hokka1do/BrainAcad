public class Store {
    String storeName;
    String storeUrl;
    String[] storeUsers;
    String authorizedStoreUser;

    void login(String name, int password) {{
        //if (name.equals(User.login) && password == User.getPassword) {

        }
    }
}
